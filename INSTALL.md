# Fanqie Lite 5 分钟安装指南

本文只适用于**已经能够正常打开 KOReader** 的 Kindle。当前文件是候选测试包说明，不代表稳定版已经发布。

## 安装前准备

- 一台已安装 KOReader 的 Kindle。
- 一根可以传输数据的 USB 线。
- 项目所有者提供的 `fanqielite-koreader-candidate-<提交号>.zip` 候选测试包。
- 首次添加书籍必须联网：开始前关闭飞行模式，连接可信的 Wi-Fi，并确认 Kindle 日期和时间基本正确。TLS 校验会在设备时间异常时安全停止。

不需要安装 App、复制 Cookie、登录番茄账号或执行终端命令。

## 0–1 分钟：解压并检查目录

在电脑上解压候选包。解压后应直接得到：

```text
fanqielite.koplugin/
├── _meta.lua
├── main.lua
└── fanqielite/
```

如果看到 `fanqielite-koreader-candidate/fanqielite.koplugin/` 或连续两层 `fanqielite.koplugin/`，不要复制外层目录，只复制包含 `_meta.lua` 的那一层。

## 1–3 分钟：复制插件

1. 先退出 KOReader，回到 Kindle 主界面。
2. 用 USB 连接电脑，打开 Kindle 磁盘。
3. 打开 `koreader/plugins/`。
4. 把整个 `fanqielite.koplugin` 文件夹复制进去，最终路径必须是：

```text
koreader/plugins/fanqielite.koplugin/main.lua
```

首次安装不需要修改 `koreader` 内的其他文件。

如果是升级测试，备份时间不计入首次安装的 5 分钟。先在电脑新建一个带日期的备份文件夹，并把以下仍然存在的项目复制进去：

```text
koreader/plugins/fanqielite.koplugin
koreader/settings/fanqielite.lua
koreader/settings/fanqielite.lua.old
koreader/fanqielite
fanqielite-bookshelf.json（如果你曾主动导出）
```

这里只复制，不要删除 Kindle 上的设置或数据。确认备份完成后，只替换 `koreader/plugins/fanqielite.koplugin` 插件目录；不要把备份文件夹本身复制进 Kindle。

## 3–4 分钟：安全弹出并重启 KOReader

1. 在电脑上安全弹出 Kindle 磁盘。
2. 拔掉 USB 线。
3. 重新打开 KOReader；如果 KOReader 原本没有退出，请执行一次完整退出后重新进入。

## 4–5 分钟：加入第一本书

1. 再次确认飞行模式已关闭且 Wi-Fi 已连接；插件不会为了首次取书而绕过系统网络确认。
2. 在 KOReader 工具菜单中打开“番茄小说（实验版）”。
3. 确认首先看到“我的本地书架”。
4. 选择“搜索或添加一本书（推荐）”。扫码导入会明确标为“尚未开放”，不会要求登录，也不会阻挡这条默认路径。
5. 输入书名、作者名，或形如 `https://fanqienovel.com/page/<书籍ID>` 的番茄官网详情页链接。
6. 选中书籍后等待目录加载；书籍详情第一项应为“开始阅读（第 1 章）”，选择后进入正文。实际打开过后，该入口才会变为“继续阅读”。

空书架会依次显示“搜索或添加一本书（推荐）”“从文件导入书架”“扫码导入我的番茄书架（尚未开放）”。已有书籍时不会继续保证三个入口相邻：常用的“搜索或添加”固定在排序项之后、书籍列表之前，文件导入和未开放扫码保留在书籍列表之后。这是为了让长书架仍能快速添加下一本，同时不让低频入口挤走日常切书区域。

官网有时会要求浏览器滑块验证。出现相关提示时，插件不会绕过验证；请稍后重试，或改用番茄官网书籍链接。首次打开未缓存章节需要联网，已经完整缓存的章节可离线打开。

如果系统正在询问是否打开 Wi-Fi，先在该系统提示中作出选择。关闭提示后若插件仍在等待，再次点按刚才的联网入口会取消这次等待，不会排入第二个请求；需要重试时再点一次。等待超过 50 秒会自动取消，随后连接成功也不会偷偷继续旧操作。真正开始读取官方页面后，进度窗口会写明“点按取消”，请直接点按该窗口终止任务。

如果读取过程中遇到临时连接失败、超时、传输不完整或番茄官方服务 5xx，插件会说明本地数据未改变，并提供“重试 / 返回”。“重试”只重新执行刚才那一本书、一次搜索或一个章节。主动取消、登录/授权、证书、404、429、滑块验证、响应过大和页面解析变化不会显示一键重试，请先按提示处理原因。

日常阅读时，章节目录会用 `[当前]` 标出插件记录的当前章节，用 `[可离线]` 标出已经完成格式与完整性检查的缓存。`[缓存需修复]` 表示本地文件存在但不完整或不可读，点开后需要联网重新获取；没有缓存标记的章节也需要联网。若目录显示“离线缓存状态不可读”，插件不会猜测缓存可用性，实际打开章节时会再次安全检查。

阅读正文时也可以从 KOReader 工具菜单再次打开“番茄小说（实验版）”。“我的本地书架”会把最近阅读书籍放在第一项，并在目录边界允许时紧接着显示带目标章号的“下一章”和“上一章”；选择即可切换，不需要设置手势。到达第一章或最后一章时，不可用方向不会显示。

书籍行显示 `[未开始 · 共 N 章]` 表示目录已经取得但还没有本机打开记录或已验证的导入进度；`[当前章/总章数]` 才表示已有阅读进度；`[待获取目录]` 表示首次阅读仍需联网。这里的“未开始”不会创建或上传番茄账号进度。

手势完全可选。在插件“设置与数据 → 可选手势快捷操作”中可以查看说明；如有需要，可在 KOReader 的手势管理中绑定“番茄小说：下一章”或“番茄小说：上一章”。动作只识别当前打开并重新校验通过的 Fanqie Lite 缓存章节，不会让普通 EPUB、PDF 或其他插件书籍跳转；未缓存目标章仍会走正常的联网、取消和错误提示。

如果暂时无法联网，仍可确认插件是否正常加载，但不能把本次操作计为“5 分钟内开始阅读”通过。请保留现有书架和缓存，网络恢复后再从书籍详情继续；不需要重新安装插件。

## 安装成功的判断

- 工具菜单中出现“番茄小说（实验版）”。
- 打开后首先显示“我的本地书架”。
- 首次空书架能依次看到搜索或添加（推荐）、文件导入、扫码导入（尚未开放）三个入口；已有书架按上文说明重新排列属于正常现象。
- 联网添加公开书籍后能显示目录；打开公开章节时正文无乱码。

## 常见问题

### 工具菜单里没有插件

先检查是否误放成双层目录。正确文件必须是 `koreader/plugins/fanqielite.koplugin/_meta.lua`，然后完整重启 KOReader。

### 提示网络、证书或设备时间错误

确认 Wi-Fi 可用，并让 Kindle 联网校准日期和时间后重试。失败不会删除现有书架或缓存。

如果刚才关闭了系统 Wi-Fi 提示，第一次重新点按可能只会取消仍在登记的等待请求；看到“已取消等待联网”后再点一次即可。插件不会并发执行两个请求，取消或超时也不会改动本地书架、阅读进度和缓存。

如果看到“重试 / 返回”，可以在确认 Wi-Fi 正常后选择“重试”；如果提示登录、证书、访问过频或官方安全验证，请先按文字说明处理，不要连续点按原入口。

### 搜索提示官方安全验证

这是番茄官网的访问限制，不是插件损坏。插件不会绕过验证；请稍后重试或输入官方书籍链接。

### 章节提示预览、登录或需要解锁

该章节没有向未登录网页提供完整公开正文。插件会拒绝保存预览或锁定内容，请在番茄官方客户端阅读该章节。

### 目录显示缓存需修复或状态不可读

`[缓存需修复]` 的章节可以在联网后点开，插件会重新读取番茄官方公开章节并安全替换损坏缓存。若显示“离线缓存状态不可读”，先返回书籍页重试；持续出现时完整重启 KOReader。插件不会因缓存路径或安全范围异常而先下载正文，也不会改变书架和阅读进度。

如果联网章节已经写入缓存、但随后无法确认 KOReader 本机阅读位置或保存继续阅读位置，插件会停止进入阅读器，并明确提示“缓存状态已改变”。此时书架和阅读进度会保持或恢复到上一版；新章节缓存可以在问题排除后复用，较早缓存可能已经按每书 12 个上限自动清理。请按屏幕提示重试，持续出现时重启 KOReader 并检查存储只读状态。

从文件导入的章节内百分比只会在 KOReader 真正完成文档初始化后应用并标记为已使用。看到“正在打开”不代表位置已经消费；如果文档异步加载失败、位置跳转失败或清理保存失败，插件会保留该位置供下次重试。已有 `.sdr` 时始终以 KOReader 本机位置为准，并在成功打开后清理外部位置。

### 提示磁盘设置状态无法确认

这表示新的主设置文件已经完成原子替换，但最终重读校验失败，而且插件自动恢复上一版也没有成功。运行中的内存书架会回到保存前状态，但磁盘上的 `koreader/settings/fanqielite.lua` 可能已经改变，两者不能再视为一致。请停止继续操作并完整重启 KOReader；不要删除或覆盖 `fanqielite.lua.old`。如果重启后书架异常，先退出 KOReader，再从升级前备份恢复；没有外部备份时保留主设置和 `.old`，不要反复打开或修改书架，提交故障报告后再处理。

### 提示目录超过安全上限

插件最多接受单书 10,000 章和 1,000 卷。超过上限时会停止本次添加或刷新；已有本地书架、阅读进度和缓存不会改变。不要修改上限后反复请求；请先在番茄官网确认目录是否正常，并把书籍链接与插件版本提交给项目维护者核对。暂时可继续阅读已经完整缓存的章节，其他内容请使用番茄官方客户端。

### 长书目录为什么先显示章段

超过 200 章时，“章节目录”会先显示“第 1–100 章”这类章段；带 `[当前]` 的章段就是当前阅读位置，点入后仍会标出当前章、`[可离线]` 和 `[缓存需修复]`。这是为老款 Kindle 控制菜单内存，不会改变章节顺序、阅读进度或缓存。200 章以内仍直接显示全部章节。

## 安全回退与卸载

升级后如需回退：先退出 KOReader，首先把 `koreader/plugins/fanqielite.koplugin` 替换为旧版插件。如果旧版能够正常显示书架和阅读，可以保留当前数据，不要额外覆盖。

如果旧版无法读取升级后的设置、书架异常或你需要完整回到升级前状态，请继续退出 KOReader，并从**同一份升级前备份**一起恢复 `koreader/settings/fanqielite.lua`、存在时的 `.old` 文件和 `koreader/fanqielite`。不要混用不同日期的代码、设置与缓存；恢复会丢弃备份时间之后新增的插件书架变化和缓存。

只卸载插件时，仅删除：

```text
koreader/plugins/fanqielite.koplugin
```

这不会自动删除 Kindle 书籍、KOReader、其他插件、Fanqie Lite 书架设置或章节缓存。完整数据清理前建议先从插件导出本地书架并把 JSON 复制到电脑；删除 `koreader/fanqielite` 会永久删除离线章节及对应 `.sdr` 阅读位置，删除 `koreader/settings/fanqielite.lua*` 会永久删除本地书架、目录和插件记录的进度。请按插件“设置与数据 → 完全卸载与安全回退”中的路径逐项操作，不要删除整个 `koreader` 目录。
