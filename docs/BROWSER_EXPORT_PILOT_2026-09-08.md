# 浏览器书架导出先导验收（2026-09-08）

状态：专用测试账号的两本未读书完成一次真实导出及桌面导入格式验证；不是扫码插件验收，也不是全场景通过。

## 来源与结果

- 用户在 Chrome 官方书架页运行开发脚本，反馈官网弹窗“已导出 2 本书”。
- 对应代码提交：`6587358f90e30b618e2758676c3694c32c23b75f`。用户复制执行，未独立取得浏览器内已执行脚本哈希，因此版本归属基于本次操作顺序。
- 下载文件 `fanqielite-bookshelf.json`：675 字节；SHA-256 为 `cd67ab7a8159f4096c3bc327e0ba675c7b24f19396466276be6448bf8b39553c`。
- 文件包含两本书，书名与现场官网书架一致，无回退占位书名；每本仅有 `id`、`title`、`cover_url` 字段，顶层为导出格式元数据及 books。
- 桌面 Node.js 解码 JSON 后，经全字节十进制转义内存管道传给 LuaJIT，调用生产 `Import.validate()`，输出 `IMPORT_VALIDATED 2`。不使用 eval，不把原始文件、封面 URL 或书籍字段值写入测试记录。
- 实际导入器的字段白名单、凭证字段拒绝、ID、文本及结构校验通过。未在 KOReader 的 rapidjson 环境重新解码，也没有写入 Kindle。

## 本次修复依据

初版只识别无版本目录，第二版错误地将官网的 `v:version` 认作须替换的数字模板。用户提供去查询参数的资源路径，证实真实请求保留字面量 `/reading/bookapi/bookshelf/info/v:version/`。测试据此先复现发现失败，再修复精确匹配；不是放开任意版本文本或域名。前两次模拟通过不能证明真实官网可用，本次下载才提供首个真实导出证据。

## 尚未证明

- 作者未导出：不能据此判断官方缺少作者，详情响应字段映射需要继续核对。
- 两本书均未读，不能验证最近章节或阅读位置同步。
- 未验证分组、多页、大书架、不同书籍类型及已失效登录。
- 该文件字段校验不等于证明所有潜在凭证值均不可能被错误映射；不声称浏览器 Cookie 已清除或临时会话已被服务器撤销。
- 官方扫码登录成功不等于 Kindle 插件扫码实现、安全审计或真机验收完成。

真实书架文件保留在用户下载目录，不提交仓库。没有操作正文、云端书架修改、Kindle 系统或用户书库。

## 2026-09-17 公开前端复核与完整性收紧

本次仅访问无需登录的公开书架页面和静态前端包，没有使用专用测试账号、浏览器会话、Cookie 或请求头。公开 HTML 的 SHA-256 为 `488a3a48ddfbdae1771c44e59280375d7002fd27ceee08373e36d8915762b86b`；页面引用的 `BookShelf.93e1261b.js` 为 `3c0ceb2b93656d7f48eeb717062e114f5bf6c71a4f04fbfb3460984040152e34`，其公共包 `muye_a2c8e2a7.js` 为 `6ab30580f1dee8f57939998a18a300ddd9d366eee270541dde20b9b26b887973`。

当前公开实现只发起一次书架信息请求，读取完整 `book_shelf_info` 后把其中全部 `book_id` 交给 `/api/book/simple/info`；生成的公开响应模型包含 `book_list`、`book_list_info`、`book_shelf_info` 和 `group_data`，未显示游标、`has_more` 或分页字段。该证据说明当前官网前端没有可照抄的分页流程，不足以证明服务端永远不会分页，也不等于真实大书架已经通过。因此导出器没有猜测未知游标，而是收紧完整性：所有受支持书籍必须具备合法 ID，详情必须与书架 ID 一一对应并提供非空书名；任何缺失、重复、额外详情或未知结构都会在下载前整体停止，不会用占位书名生成残缺备份。
