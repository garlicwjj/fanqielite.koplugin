# Changelog

本项目遵循语义化版本号。这里记录每个公开版本中已经完成并验证的变化。

## 未发布

- 超过 200 章的目录改为每 100 章一个章段，进入后才创建该段章节菜单；万章目录根菜单最多 100 项，同时保留当前章定位、离线/损坏状态和绝对章节跳转。
- 官网目录最多接受单书 10,000 章和 1,000 卷；恰好位于边界的长书保持兼容，超限响应会在本地书架更新与设置保存前整体停止，保留已有目录、进度和缓存。
- 新增可选的阅读器上一章/下一章动作，供用户自行绑定 KOReader 手势；动作从当前安全缓存识别书籍和章节，不依赖最近阅读状态，也不会作用于普通文档。
- 临时传输失败会在原处提供明确的“重试 / 返回”，并只重跑同一用户操作；取消、登录/授权、证书、404、429、安全验证和解析变化保持安全停止，避免无意义请求。
- 新增可保存多本书的本地书架，每本书独立保留目录和最后阅读章节。
- 旧版单书设置会自动迁移，并继续同步旧字段以支持降级回 0.1.0。
- 支持按最近阅读、书名和最近添加排序；刷新目录时尽量跟随原章节 ID。
- 新增单本书移除与缓存清理确认框；两项操作彼此独立。
- 首次使用页面展示扫码导入、添加书籍和文件导入三个入口；未完成的导入能力明确标为开发中，不接触账号。
- 首次使用入口明确标出扫码“尚未开放”和搜索/添加“推荐”；刚加入且尚未打开的书在首页与详情页统一显示“开始阅读”，只有本机或导入进度存在时才显示“继续阅读”。
- 本地书架首页为最近阅读书籍直接提供带目标章号的上一章/下一章；书籍详情使用相同文案，回调固定使用渲染时的目标章节，第一章和末章不显示无效方向。
- 新增本地书架迁移、进度、排序、输入清洗及缓存删除边界测试。
- 设置序列化、创建、写入、同步、关闭和原子替换失败现在只返回固定阶段分类；异常对象不会被字符串化；写入前会安全移除并复查既有临时文件或链接，失败仍保留磁盘旧设置并恢复上一版内存书架。
- 启动时会比较原设置与白名单状态投影；普通未知字段在原始备份后清理，检测到账号或会话字段时则只用清洗状态覆盖主设置与 `.old`，绝不复制敏感原值，失败会明确说明原文件可能尚未更新。
- 章节缓存创建、写入、同步、关闭、替换、读取、列目录和删除失败现在只返回固定中文阶段或失败数量；缓存根必须精确属于 KOReader 数据目录，计数与淘汰只接受安全范围内的普通数字 XHTML 文件；写入前安全移除并复查既有临时路径，删除仍严格限定为插件自己的数字命名缓存。
- 联网章节缓存已经写入后，若本机阅读位置检查或进度保存失败，提示会如实区分已改变的缓存与已回滚的书架/阅读进度，并保留旧缓存自动清理警告，不再错误声称缓存没有改变。
- 章节目录会完整验证至多 12 个候选缓存，并标出当前章节、可离线章节和需要联网修复的损坏缓存；缓存目录或真实路径无法安全确认时不发起网络恢复，也不宣称章节可离线。
- 网络层拒绝非官方地址和所有重定向，限制响应为 1 MB；使用 KOReader 自带 CA 集验证证书链，并独立校验 SAN 主机名，缺失或不匹配时安全失败；提供超时、证书、限流、登录限制及服务异常的可操作中文提示。PW3 真实握手仍待集中实机验证。
- 公开网络失败不再回显 socket 原始错误或字符串化异常对象；子进程拒绝非字符串错误，主界面只显示内部标记的可信提示，未预期异常使用固定恢复说明。
- 新增严格的本地 JSON 书架导入：限制文件名、版本、256 KB 大小、500 本数量及字段长度；书籍与章节 ID 在输入、官方解析、搜索、迁移、缓存和浏览器导出中统一限制为 10–64 位数字字符串；重复 ID 和字段拒绝不会回显原值。
- JSON 中的 `current_chapter_title` 和 `reading_position` 现在必须与合法 `current_chapter_id` 一起提供；纯书架记录仍可省略全部进度字段，只有章节 ID 的记录也保持兼容，避免成功提示后静默丢弃孤立进度。
- 封面地址在浏览器导出、JSON 导入和旧设置恢复中统一要求为结构完整的 HTTPS URL；用户信息、片段、空主机、空白/控制字符、异常端口和 Cookie/Token/session/CSRF 等敏感查询键会被拒绝或清理，普通 CDN 签名参数保持兼容。
- 启动清理旧设置时，不安全封面地址不再进入 `fanqielite.lua.old`；设置持久化会在创建主文件或备份前统一拒绝含凭证参数、用户信息或其他非法结构的封面地址。
- 添加书籍只接受纯数字 ID 或精确的 `https://fanqienovel.com/page/<ID>` 官方详情页（含官网 `www` 跳转主机），可保留查询参数/片段；伪装域名、HTTP、用户信息、端口、额外路径、章节链接和任意 `bookId=` 文本会拒绝，且不会误送到书名搜索。
- JSON 导入文件的打开、大小确认、定位、读取和关闭失败现在使用固定中文阶段；异常对象、底层路径和系统文案不会进入界面，只有完整读取并安全关闭后才会开始解析或修改书架。
- JSON 导入先显示新增与更新数量并要求确认；不会覆盖已有目录、缓存或本地阅读进度。
- 文件导入只记住非空、不超过 1024 字节且不含 NUL/C0/DEL 控制字符的目录；异常目录不会覆盖上次安全位置，也不会阻止已经验证通过的书架内容按原事务保存。
- 新增浏览器端导出器原型：只允许番茄官网书架页、同源精确只读接口和 2 MB 流式响应上限，不读取 Cookie 内容，仅输出 JSON 白名单字段；原始异常、服务消息和响应正文不会进入弹窗或控制台。
- 浏览器导出现在要求每个受支持的书架条目都有合法 ID、唯一详情和非空书名；任何缺项、重复、未知结构或占位书名风险都会在可选进度请求和文件下载前整体停止，避免生成看似成功但静默漏书的备份。
- 已发现的浏览器阅读进度响应现在限制为 500 项并逐项验证对象、书籍 ID、导出书籍的唯一记录及章节 ID；章节标题兼容当前公开 `ApiItemInfo` 的 `origin_chapter_title`/`title`，但在三个进度比例字段的真实量纲确认前不会猜测或导出章节内位置。
- 本地书架 JSON 导出会在写入前拒绝稀疏、混合键或畸形的内存书架，避免生成静默缺书的备份；创建、写入、同步、关闭、重读、校验和原子替换失败使用固定中文阶段，既有临时文件或链接会先安全清理并复查。
- 新增固定白名单的候选测试包构建、提交号追踪、SHA-256 校验和、包结构自动检查及 5 分钟新手安装指南；不自动发布候选包。
- 新增一次性扫码导入独立威胁模型和设置持久化凭证字段硬门禁；真实 Passport 接口仍未实现或启用。
- 新增端点无关的一次性会话状态机原型，以合成数据验证并发门禁、取消、过期、总超时、退出失败、迟到结果隔离和敏感引用清理；尚未接入界面或真实账号。
- 新增基于 KOReader 原生 `QRMessage` 的内存二维码显示适配层，主动拒绝超长内容以避免上游静默截断，并在关闭后清除文本引用；尚未接入扫码流程或真机验证。
- 新增 KOReader 可取消子进程 IPC 安全审计和简单字符串匿名管道门禁，固定错误与 256 KB 上限避免凭证进入复杂序列化日志；真实网络客户端仍未接入。
- 新增未启用的端点无关敏感 HTTPS 客户端：复用 CA 与 SAN 校验，只允许代码内固定操作白名单、官方主机、精确路径/方法和受限内存凭证头，禁止代理、重定向、动态 URL、文件与日志；尚无真实端点、账号或界面接入。
- 新增未启用的最小书架匿名管道门禁：子进程结果必须通过现有 JSON 字段/凭证验证、标准化编码和编码后复验，父进程再次解码验证；原始响应、Cookie 字段和异常不能直接越过 IPC。
- 收紧未启用的一次性会话内存凭证：只复制受限的 Cookie、授权头、CSRF 和退出票据，拒绝未知字段、独立 sessionid、控制字符和超长值；调用方后续修改原表不会改变会话，退出回调返回后会清空传入凭证表。
- JSON 解码/编码失败不再拼接 rapidjson 原始异常或输入附近内容；本地书架文件、导出器和番茄官方响应使用固定提示，官方搜索只显示验证后的数字状态码，避免在屏幕或日志回显原始内容。
- 官方搜索状态码只接受有限的 32 位整数或十进制整数字符串；对象、NaN、无穷、小数和其他畸形值固定安全失败，不显示为可信服务代码，也不会触发异常对象的字符串转换。
- 书籍、目录与章节解析取消不可信字段的隐式字符串/数字转换：ID 必须是数字字符串，文本和正文必须是字符串，序号与字数必须是有限整数，权限标志只接受明确布尔表示；未知权限结构会在生成缓存前整体拒绝。
- `INITIAL_STATE` 页面提取只接受不超过 1 MB 的字符串；对象不会被隐式 `tostring()` 执行，非字符串和超限页面使用固定安全错误并在解析前停止。
- 官方书名和章节标题限制为 300 字节、作者为 150 字节并拒绝控制字符；章节正文限制为 1 MB，只允许 XML 合法的 Tab、换行和回车控制字符，避免异常网页污染菜单、设置或 XHTML。
- 卷目录、卷内章节、平铺章节和章节 ID 列表必须是从 1 开始的连续纯数组；空洞、非整数键或混入字段会整体拒绝，避免用被 `ipairs` 静默截断的目录覆盖本地完整目录。
- 正文纯文本提取会以大小写无关且标签名有边界的规则移除完整 `script`/`style` 块；残留的未闭合活动标签会整体拒绝，脚本或样式文本不能再凑够公开正文长度并绕过短预览判断。
- 正文出现明确的 `hidden`、`aria-hidden=true`、`display:none` 或 `visibility:hidden` 标记时整体拒绝；隐藏块文字不能计入公开正文长度，普通 hidden 文本及明确可见样式仍正常解析。
- 完整 HTML 注释会在去标签前移除，残留或未闭合注释边界会安全拒绝；注释中包含 `>` 的长隐藏文字不能再被提前截断后计入正文，转义注释标记仍作为可见文本保留。
- HTML 标签扫描会跳过引号属性中的 `>`，属性值不会泄露进正文或帮助短预览越过长度门槛；位于这类属性后的真实 `hidden` 仍会识别，普通属性值里的 hidden 单词不会被误判。
- 兼容矩阵的公开正文正确率现在同时要求解析成功、缓存已保存、无乱码、无未知 PUA、可见正文至少 500 字且不低于官方声称字数的 45%；不完整或未落盘结果不再虚增 95% 发布门禁。
- 兼容证据工具新增离线结构指纹模式，只哈希字段名、数据类型和数组元素结构；不同正文或账号值不会改变指纹或进入输出，畸形、超大、过深或节点过多输入会固定拒绝。
- 首次书架继续固定显示扫码、搜索/添加和文件导入三个入口；点击尚未开放的实验性扫码会明确说明当前未发起账号授权、未请求或保存登录信息，并引导新用户改用两个已可用的安全入口。
- 候选安装指南不再只备份旧插件代码：升级前会同时复制插件、设置、上一版设置、缓存/KOReader 阅读位置及可选导出文件；完整回退要求使用同一份快照，并明确会丢弃备份后的插件数据变化。
- 设置页会明确区分“仅停用插件”和“永久删除数据”：完整清理前建议先导出书架，并分别说明缓存目录会带走离线章节及 `.sdr` 位置、设置文件会带走本地书架/目录/进度；隐私页同时标明扫码当前未开放且未来也不会保存账号登录。
- 固定 2026-08-31 番茄官网公开登录脚本快照：记录 `aid=2503`、`appName=muye` 及二维码生成、轮询和退出的公开路径；未调用端点或创建会话，响应字段、Cookie 与退出语义验证前仍禁止开放真实扫码。
- KOReader 网络管理器、可取消任务包装或复位自身异常时，插件现在会清除忙碌状态并显示固定恢复说明；异常对象、路径和可能的敏感内容不会逃出到界面。
- 公开请求与未启用的一次性授权请求现在也隔离 KOReader 全局网络超时的配置和复位异常；配置失败不会发起请求并会尽力复位，复位失败会丢弃响应、隐藏异常对象并要求重启 KOReader，避免在未知超时状态下继续联网。
- 加载部分损坏的本地设置时，书架和章节数组不再被 `ipairs` 在第一个空洞静默截断；所有可恢复的正整数索引项会按顺序保留，自动规范化前的原始设置会写入 `.old` 备份。
- 加载损坏设置时会拒绝并修复数组索引、章节位置、章节序号和书架时间戳中的 NaN、正负无穷及负时间，避免异常数值破坏排序、继续阅读或设置写入后的重读校验。
- 已保存的导入阅读位置只保留 0–1 的有限数值；兼容的数字字符串会规范为 number，NaN、无穷、越界值、非对象进度和无效章节 ID 会移除并触发写回修复。
- 从文件导入的章节内位置现在只在 KOReader 发出 `ReaderReady`、且位置应用成功或本机 sidecar 已确认优先后才消费；异步文档加载、`GotoPercent` 或清理保存失败都会保留可重试位置，并由新 ReaderUI 的活动插件实例完成事务。
- 本地书架查找只接受统一规则验证后的数字字符串 ID；对象和 Lua number 不再被隐式字符串化后命中书籍，查找、进度更新和移除入口共用该门禁。
- 损坏设置中的书名、作者、章节标题、导入进度标题和封面地址现在复用长度及控制字符门禁；不安全文本会使用固定安全值或被移除，并触发规范化写回。
- 从本地书架移除书籍后会另行询问是否立即清理该书章节缓存，不再承诺一个移除后实际不存在的稍后入口；保留缓存可供重新添加时复用，清理时仍保留 `.sdr` 和未知文件。
- 缓存读取、写入、自动淘汰和清理现在都会比较缓存根目录与数字书籍目录的解析后真实路径；书籍目录若通过符号链接超出插件根目录，所有操作都会安全拒绝。

## 0.1.0 - 2026-08-13

第一个可安装的实验版本：

- 支持输入番茄小说官方书籍链接或书籍 ID。
- 从官方网页读取书籍信息、章节目录和公开章节。
- 解码当前已知的 PUA 字符混淆；遇到未知映射时拒绝保存乱码。
- 将章节保存为 XHTML，并使用 KOReader 原生阅读引擎打开。
- 提供继续阅读、章节目录、上一章和下一章入口。
- 每本书最多保留 12 个章节缓存。
- 拒绝登录墙短预览、锁定章节和章节 ID 不匹配的响应。
- 通过 Lua 5.1 语法检查、核心解析测试及官方网页只读烟雾测试。
- 2026-08-16 在 Kindle Paperwhite 3（5.16.2.1.1、KOReader 2026.03）完成实机验证：插件加载、88 章目录、公开章节解码及翻页均正常。
